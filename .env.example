# Groq — NL parsing & explanation only (never for recommendations)
GROQ_API_KEY=

# Bright Data — live market data scraping
BRIGHT_DATA_API_TOKEN=
BRIGHT_DATA_ZONE=web_unlocker1

# Optional: force mock data even when Bright Data is configured
USE_MOCK_MARKET_DATA=false
